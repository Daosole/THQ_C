#include <stdio.h>
#include <string.h>
#define N 100
int main(void)
{
    void swap(char *, char *);
    char s1[N], s2[N], s3[N];
    char *a, *b, *c;
    a = s1, b = s2, c = s3;
    int i;
    printf("Input three strings:\n");
    gets(s1);
    gets(s2);
    gets(s3);
    

}